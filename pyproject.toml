[project]
name = "nexkit"
version = "0.1.0rc1"
description = "Agents. Skills. One workflow."
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

